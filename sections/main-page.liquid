<section class="section" aria-label="Page">
  <div class="page-width" style="max-width:780px">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "tag": "section",
  "settings": [],
  "enabled_on": { "templates": ["page"] }
}
{% endschema %}
