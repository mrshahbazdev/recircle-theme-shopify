<section class="section editorial-page collection-page" aria-label="Collection">
  <div class="page-width">
    <header class="editorial-page__header">
      <div class="editorial-page__heading-group">
        <p class="editorial-page__eyebrow">
          <svg width="14" height="14" aria-hidden="true" focusable="false"><use href="#icon-leaf"/></svg>
          <span>{{ section.settings.eyebrow | default: 'Refurbished collection' }}</span>
        </p>
        <h1 class="editorial-page__heading">{{ collection.title }}</h1>
        {%- if collection.description != blank -%}
          <div class="editorial-page__subheading">{{ collection.description }}</div>
        {%- endif -%}
        <p class="editorial-page__meta">
          {{ 'collections.general.showing_count' | t: count: collection.products.size, total: collection.all_products_count }}
        </p>
      </div>
    </header>

    <div class="facets-wrap">
      <aside class="facets" aria-label="Filters">
        {%- if section.settings.show_condition_filter -%}
          <div class="facets__group">
            <h3>Condition</h3>
            <ul>
              {%- assign grade_list = 'a,b,c,d' | split: ',' -%}
              {%- for grade in grade_list -%}
                <li>
                  <label>
                    <input type="checkbox" name="filter.p.m.recircle.condition_grade" value="{{ grade }}"
                      {% if current_tags contains grade %}checked{% endif %}>
                    Grade {{ grade | upcase }}
                  </label>
                </li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}

        {%- if collection.all_vendors.size > 1 -%}
          <div class="facets__group">
            <h3>Brand</h3>
            <ul>
              {%- for vendor in collection.all_vendors limit: 10 -%}
                <li><label><input type="checkbox" name="filter.p.vendor" value="{{ vendor | escape }}">{{ vendor }}</label></li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}

        <div class="facets__group">
          <h3>Price</h3>
          <ul>
            <li><label><input type="checkbox" name="filter.v.price" value="0-300"> Under €300</label></li>
            <li><label><input type="checkbox" name="filter.v.price" value="300-600"> €300 – €600</label></li>
            <li><label><input type="checkbox" name="filter.v.price" value="600-1000"> €600 – €1000</label></li>
            <li><label><input type="checkbox" name="filter.v.price" value="1000-"> €1000+</label></li>
          </ul>
        </div>
      </aside>

      <div>
        <div class="collection-toolbar">
          <p class="collection-toolbar__count">
            {{ 'collections.general.showing_count' | t: count: collection.products.size, total: collection.all_products_count }}
          </p>
          <label class="collection-toolbar__sort">
            <span>{{ 'collections.general.sort_by' | t }}</span>
            <select aria-label="Sort">
              <option value="manual">Featured</option>
              <option value="price-ascending">Price, low to high</option>
              <option value="price-descending">Price, high to low</option>
              <option value="created-descending">Newest</option>
            </select>
          </label>
        </div>

        <div class="collection-grid">
          {%- if collection.products.size > 0 -%}
            {%- paginate collection.products by 24 -%}
              {%- for product in collection.products -%}
                {% render 'product-card', product: product, show_vendor: true %}
              {%- endfor -%}
              {%- if paginate.pages > 1 -%}
                <nav class="pagination" style="grid-column:1/-1" aria-label="Pagination">
                  {%- if paginate.previous -%}<a href="{{ paginate.previous.url }}">← {{ 'general.pagination.previous' | t }}</a>{%- endif -%}
                  {%- for part in paginate.parts -%}
                    {%- if part.is_link -%}
                      <a href="{{ part.url }}">{{ part.title }}</a>
                    {%- else -%}
                      {%- if part.title == paginate.current_page -%}
                        <span class="active">{{ part.title }}</span>
                      {%- else -%}
                        <span>{{ part.title }}</span>
                      {%- endif -%}
                    {%- endif -%}
                  {%- endfor -%}
                  {%- if paginate.next -%}<a href="{{ paginate.next.url }}">{{ 'general.pagination.next' | t }} →</a>{%- endif -%}
                </nav>
              {%- endif -%}
            {%- endpaginate -%}
          {%- else -%}
            <div class="editorial-empty" style="grid-column:1/-1">
              <div class="editorial-empty__icon" aria-hidden="true">
                <svg width="30" height="30"><use href="#icon-search"/></svg>
              </div>
              <h2 class="editorial-empty__title">No matches yet</h2>
              <p class="editorial-empty__body">{{ 'collections.general.no_products' | t }}</p>
              <a href="{{ routes.all_products_collection_url }}" class="button button--primary editorial-empty__cta">Browse everything</a>
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Product grid",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Refurbished collection" },
    { "type": "checkbox", "id": "show_condition_filter", "label": "Show condition-grade filter", "default": true },
    { "type": "range", "id": "products_per_page", "label": "Products per page", "min": 8, "max": 48, "step": 4, "default": 24 }
  ],
  "enabled_on": { "templates": ["collection"] }
}
{% endschema %}
